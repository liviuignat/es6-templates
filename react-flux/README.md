# ReactJS 

Useful commands: 

```
npm install               // install node dependencies

gulp                      // the default task is 'gulp watch'

gulp serve:production     // serve in production configuration

gulp test                 // test it

gulp build                // development build

gulp build:production     // production build

```
